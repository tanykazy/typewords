import { BoardDirective } from './board.directive';

describe('BoardDirective', () => {
  it('should create an instance', () => {
    const directive = new BoardDirective();
    expect(directive).toBeTruthy();
  });
});
